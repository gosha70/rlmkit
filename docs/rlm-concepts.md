# RLM: Concepts, Original Research, and What RLMKit Adds

This document explains the Recursive Language Model (RLM) paradigm — its motivation, core mechanisms, and published results — and then describes how RLMKit extends and productionizes the original research.

---

## 1. The Problem: Context Window Limits and Context Rot

Modern LLMs have finite context windows. Even within those limits, quality degrades steeply as prompts grow — a phenomenon the original paper calls **context rot** (Hong et al., 2025). Prior approaches fall into two categories, both inadequate for truly large inputs:

| Approach | How it works | Why it falls short |
|----------|--------------|-------------------|
| Summarization / compaction | Iteratively compress the input | Lossy — assumes details can be safely forgotten; fails on dense multi-hop tasks |
| RAG / tool-use agents | Load snippets into the context window | Still bounded by context size; cannot express loops or recursive decomposition |

The RLM paper (Zhang, Kraska, Khattab — MIT CSAIL, [arXiv:2512.24601](https://arxiv.org/pdf/2512.24601), December 31, 2025) asks: *Can the effective context size of any LLM be scaled by orders of magnitude at inference time, without retraining?*

---

## 2. The Core Insight: Prompt-as-Variable

Instead of passing the user document into the LLM context, RLM loads it into a persistent Python [REPL](https://realpython.com/ref/glossary/repl/) as a variable. The LLM never sees the raw document — it only sees constant-size metadata (length, short prefix, available navigation functions). It then writes Python code to explore the document:

```python
# The LLM generates code like this to navigate content
section = peek(prompt, start=5000, length=500)   # read a slice
hits = grep(prompt, pattern="revenue")           # search by keyword
chunks = chunk(prompt, size=1000)                # split into segments
FINAL_VAR = "The revenue in Q3 was..."           # set the answer
```

When `FINAL_VAR` is set, the loop terminates and returns its value. This single design choice enables:

- **Unbounded input** — the document is never fed into the LLM context window
- **Unbounded output** — the answer is built in REPL variables, not via autoregressive generation
- **Symbolic recursion** — the LLM can call itself (`sub_RLM`) on programmatically constructed slices

---

## 3. The RLM Loop (Algorithm)

```
Input:  document P, query Q
Output: answer Y

state  ← InitREPL(prompt=P)           # load document into REPL env
state  ← AddFunction(state, sub_RLM)  # inject recursive self-call
hist   ← [Metadata(state), Q]

while True:
    code           ← LLM(hist)         # generate Python code
    (state, stdout) ← REPL(state, code) # execute in persistent REPL
    hist           ← hist + code + Metadata(stdout)
    if state[FINAL_VAR] is set:
        return state[FINAL_VAR]
```

Key properties of each iteration:
1. LLM receives history of `(code, trimmed_stdout)` pairs — not the document
2. LLM produces Python that either explores further or sets `FINAL_VAR`
3. REPL state (all variables) persists across iterations
4. Only constant-size metadata of stdout appended to history (not raw output)

---

## 4. Symbolic Recursion: Where the Power Lives

The critical differentiator from earlier agent frameworks is **symbolic recursion**: the LLM can invoke itself *inside code*, enabling patterns no static agent graph can express:

```python
# Map-reduce over document chunks (generated by the LLM)
results = []
for chunk in chunk(prompt, size=2000):
    summary = sub_RLM(chunk, "What is the main claim here?")
    results.append(summary)
FINAL_VAR = synthesize(results)
```

This allows:
- **Linear scans** — O(|P|) work when every chunk must be processed
- **Quadratic comparisons** — O(|P|²) when pairs of chunks must be compared
- **Multi-hop reasoning** — follow entity references across sections
- **Conditional branching** — search first, then dig deeper only into relevant sections

---

## 5. Budget Controls and Graceful Degradation

RLM imposes hard limits per run:

| Budget dimension | What it caps |
|-----------------|-------------|
| `max_steps` | Number of LLM calls in the loop |
| `max_tokens` | Total tokens across all calls |
| `max_cost` | Total spend in USD |
| `max_time_seconds` | Wall-clock time |
| `stall_limit` | Consecutive steps with no code and no FINAL answer (default 3) |

When any limit is hit, the system returns the best partial answer accumulated so far rather than raising an unhandled exception. This is essential for production: a long-running trace should degrade gracefully, not crash.

> **Model compatibility note:** models that don't follow the `FINAL:` protocol (e.g. some Ollama models) are handled by the stall circuit-breaker, which accepts the last substantive prose response when `stall_limit` is reached.

---

## 6. Multi-Model Cost Optimization

The root call (which reasons and plans) can use a stronger model, while recursive sub-calls (which retrieve and transform) use a cheaper model. The paper demonstrated this with GPT-5 (root) + GPT-5-mini (sub-calls):

```
Root:     GPT-5         — complex planning, synthesis
Sub-call: GPT-5-mini    — retrieval, transformation, filtering
```

This achieves near-root-quality answers at sub-call cost for the bulk of the token budget.

---

## 7. Published Benchmark Results

The paper evaluated on four tasks of increasing complexity:

| Benchmark | Scale | Complexity | Task |
|-----------|-------|-----------|------|
| S-NIAH | 8K–1M tokens | O(1) | Find a specific phrase in unrelated text |
| BrowseComp+ | 6M–11M tokens (1,000 docs) | O(1) multi-hop | Multi-hop QA across documents |
| OOLONG | 131K tokens | O(n) | Semantic transformation over every line |
| OOLONG-Pairs | 32K tokens | O(n²) | Aggregating pairs of chunks |

**Selected results (GPT-5 root, GPT-5-mini sub-calls):**

| Method | S-NIAH | BrowseComp+ | OOLONG | OOLONG-Pairs |
|--------|--------|-------------|--------|--------------|
| Base GPT-5 (context limit hit) | 24.0 | 0.0 | 44.0 | 0.1 |
| CodeAct + BM25 | 22.0 | 51.0 | 38.0 | 24.7 |
| Summary agent | 58.0 | 70.5 | 46.0 | 0.1 |
| **RLM** | **62.0** | **91.3** | **56.5** | **58.0** |

Key observations:
- RLM outperforms all baselines, most dramatically on quadratic tasks (OOLONG-Pairs: 58.0 vs 0.1 for summary agent)
- Sub-calls matter most for information-dense tasks (+10–59% over REPL-only)
- Median cost is comparable to base model runs; 3x cheaper than summary agent
- Fine-tuning on just 1,000 RLM trajectories improves a small model (Qwen3-8B) by 28.3% median

---

## 8. The Reference Implementation ([alexzhang13/rlm](https://github.com/alexzhang13/rlm))

The paper's authors published a reference implementation (`pip install rlms`). It faithfully implements the algorithm with:

- **Multiple REPL environments**: local exec, Docker, Modal (documented); additional cloud sandbox adapters present in the codebase
- **TCP socket server**: isolated environments call back to the host LLM handler via a 4-byte-length-prefix JSON protocol
- **LLM client adapters**: OpenAI, Anthropic, and Portkey/vLLM documented; additional adapters in the codebase
- **Custom tools injection**: user-defined functions added to the REPL namespace
- **RLMLogger**: trajectory capture in `.jsonl` + Node.js visualizer

The reference implementation is a research artifact — excellent for reproducing paper results, but not designed for multi-provider production use, team experiments, or web-based comparison workflows.

---

## 9. What RLMKit Adds

RLMKit is an independent, production-oriented reimplementation that extends the research in several directions:

### 9.1 Clean Architecture

The reference implementation is a flat research codebase. RLMKit applies Clean Architecture (domain → application → infrastructure) with strict dependency inversion:

```
domain/         # Entities, value objects, port Protocols — zero external deps
application/    # Use cases: RunRLMUseCase, RunDirectUseCase, RunRAGUseCase
infrastructure/ # Adapters: LiteLLMAdapter, RestrictedSandboxAdapter, SQLiteStorageAdapter
server/         # FastAPI REST + WebSocket server
api.py          # Public interact() / complete() surface
```

This makes individual components testable in isolation and swappable without touching business logic.

### 9.2 Provider Abstraction via LiteLLM

The reference impl has 5 hand-written provider clients. RLMKit uses a single `LiteLLMAdapter` that covers **100+ providers** (any model LiteLLM supports) without custom client code. Switching from GPT-4o to Claude to Llama is a one-line config change.

### 9.3 Multi-Strategy Execution

RLMKit adds strategies the original paper does not implement:

| Mode | Description |
|------|-------------|
| `direct` | Full context in a single LLM call (optimal for small inputs) |
| `rlm` | Full recursive loop per the paper |
| `rag` | Embed content into an in-memory vector store; retrieve relevant chunks per query |
| `compare` | Run RLM and Direct **concurrently** (parallel threads / async gather), return metrics for both |
| `auto` | Select Direct (< 8K tokens), RAG (8K–100K), or RLM (> 100K) automatically |

### 9.4 Security-First Sandbox

The reference impl defaults to in-process `exec()`. RLMKit's default sandbox uses **RestrictedPython** — a compile-time AST transform that blocks dangerous operations (file I/O, network, subprocess, OS access) without requiring Docker. A Docker adapter is also provided for higher isolation requirements.

### 9.5 Unified Public API

```python
from rlmkit import interact

result = interact(
    content=document,
    query="What is the main finding?",
    mode="auto",            # direct / rlm / rag / compare / auto
    provider="anthropic",
    model="claude-sonnet-4-6",
    max_steps=16,           # RLM iteration budget
    max_tokens=50000,       # total token budget
    temperature=0.3,
    verbose=True,
)

print(result.answer)
print(f"Tokens: {result.total_tokens:,}")   # input + output
print(f"Cost:   ${result.total_cost:.4f}")
print(f"Steps:  {result.steps}")            # LLM calls made
```

A single function call selects the right strategy, runs it, and returns a typed result with flat metric accessors. The `metrics` dict remains accessible for backward compatibility.

### 9.6 RLM Studio — Web UI for Experiments

The reference impl includes a lightweight trajectory visualizer. RLMKit ships a full production web application (Next.js 16 + React 19 + shadcn/ui):

| Feature | RLM Studio |
|---------|-----------|
| Multi-provider chat | Run the same query through OpenAI, Anthropic, Ollama simultaneously |
| Mode comparison | Side-by-side Direct vs RLM output and metrics |
| Dashboard | Session-level charts: tokens, cost, latency, token savings |
| Trace inspector | Step-by-step view of every LLM call, generated code, and execution output |
| Budget controls | Per-session caps on tokens, cost, steps, time — configurable from UI |
| Profiles | Save and reuse provider + budget configurations |
| Dark/light theme | Full theme support |

### 9.7 Persistence and Session Management

RLMKit adds SQLite-backed conversation storage via the `StoragePort` abstraction:
- Sessions persist across restarts
- Full execution traces stored and retrievable
- Conversations searchable by session ID

### 9.8 Async and Streaming

The `server/` layer exposes both REST and WebSocket endpoints. `interact_async()` and `complete_async()` mirror the sync API. The WebSocket connection streams partial responses token-by-token to the UI.

---

## 10. Limitations and Open Work

Items from the paper not yet addressed in RLMKit, or areas for future development:

| Item | Status |
|------|--------|
| RAG mode | Available in v1.0.0 (lightweight in-memory: `LiteLLMEmbeddingAdapter` + `SQLiteStorageAdapter`); `rag-core` package integration planned for v1.1.0 |
| Recursion depth > 1 in public API | Depth is configurable in core; not exposed in `interact()` |
| Asynchronous / parallel sub-calls | Sequential only (paper limitation acknowledged) |
| Fine-tuned RLM model support | Architecture is model-agnostic; fine-tuned model can be passed as `model=` |
| Evaluation harness (recall@k, LLM-as-judge) | `eval/` directory scaffolded; harness not complete |

---

## References

- Zhang, A. L., Kraska, T., & Khattab, O. (2025). *Recursive Language Models*. arXiv:2512.24601v2. Published December 31, 2025.
- Reference implementation: https://github.com/alexzhang13/rlm
- LiteLLM: https://docs.litellm.ai/
- RestrictedPython: https://restrictedpython.readthedocs.io/
